# build output
dist/
# local preview screenshots (dev scratch)
.preview-shots/
# raw writeup originals staged for enrichment (working space)
writeups-inbox/
# personal HTB study vault (course notes, Academy PDFs) — never publish
HTB/
# generated types
.astro/
# deps
node_modules/
# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
# environment variables
.env
.env.production
# macOS
.DS_Store
# jetbrains / vscode
.idea/
.vscode/
# claude code local permission grants (machine-specific)
.claude/settings.local.json
# raw obsidian media dump (staging); served copies live in public/media/
media/
